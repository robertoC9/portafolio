# Variables de entorno: contienen el numero de WhatsApp y las claves de los
# proveedores de envio. Nunca deben subirse al repositorio publico.
.env
.env.local

# Bandeja privada del chatbot: contiene los mensajes que envian las visitas.
# Nunca debe subirse al repositorio.
mensajes-chatbot.jsonl
mensajes-whatsapp.txt
